{
  "name": "@oclushion/policy-runtime",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm --filter @oclushion/shared build && tsc -p tsconfig.build.json",
    "evaluate": "pnpm run build && node dist/evaluate-cli.js",
    "lint": "eslint src test",
    "test": "pnpm --filter @oclushion/shared build && vitest run",
    "typecheck": "pnpm --filter @oclushion/shared build && tsc --noEmit"
  },
  "dependencies": {
    "@oclushion/shared": "workspace:*"
  },
  "devDependencies": {
    "@oclushion/config": "workspace:*"
  }
}
