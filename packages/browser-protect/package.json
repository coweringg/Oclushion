{
  "name": "@oclushion/browser-protect",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm --filter @oclushion/shared build && tsc -p tsconfig.build.json",
    "lint": "eslint src test",
    "test": "vitest run",
    "typecheck": "pnpm --filter @oclushion/shared build && tsc --noEmit"
  },
  "dependencies": {
    "@oclushion/shared": "workspace:*"
  },
  "devDependencies": {
    "@oclushion/config": "workspace:*"
  }
}
